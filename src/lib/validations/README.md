# lib/validations

Zod schemas for form input and API payload validation, shared between
client-side forms (via React Hook Form) and server-side handlers.
Not implemented yet — will grow alongside each feature in later phases.
